(ns bracketbird.dispatcher
  (:require [bracketbird.contexts.context :as ctx]))



(defn create-tournament [])


(defn add-team [t-ctx]
  (let [t (ctx/get-data t-ctx)]
    ;depening on state warn
    )





  )


(defn update-team-name [t-ctx name])